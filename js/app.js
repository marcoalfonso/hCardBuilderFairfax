/**
 * AngularJS module to process the form.
 */
angular.module('hCard', [])
  .controller('data', function ($scope, $http) {

    // Form submit handler.
    $scope.submit = function(form) {    
    };
  });